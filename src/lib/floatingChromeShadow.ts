/**
 * Shared elevation for floating tab bar pill, tab actions, and basket FAB.
 * Visual matches the tab bar pill (`drop-shadow` → unified as `box-shadow` in CSS).
 */
export const FLOATING_CHROME_SHADOW_CLASS = 'floating-chrome-shadow' as const
