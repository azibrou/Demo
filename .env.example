# Optional — Maps Embed API for DineOut (https://console.cloud.google.com/google/maps-apis).
# Without a key, DineOut uses a generic Google Maps embed URL (fine for local demo).
# VITE_GOOGLE_MAPS_API_KEY=
