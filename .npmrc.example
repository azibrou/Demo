# Copy to ~/.npmrc (or merge) for GitHub Packages access to @bolteu/*.
# Token needs read:packages and bolteu org SSO authorization.
#
# @bolteu:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN
